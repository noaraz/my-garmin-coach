# Generate secrets: openssl rand -hex 32
JWT_SECRET=change-me-to-random-32-chars
GARMINCOACH_SECRET_KEY=change-me-to-different-random-32-chars
DATABASE_URL=sqlite+aiosqlite:////data/garmincoach.db
ENVIRONMENT=development
# Production only — set to your Render URL
# CORS_ORIGINS=https://your-app.onrender.com

# Bootstrap (one-time admin user creation)
# Generate with: openssl rand -hex 32
BOOTSTRAP_SECRET=
# Your app's public URL (no trailing slash), used to construct invite links
APP_URL=
